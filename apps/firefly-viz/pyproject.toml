[project]
name = "firefly-viz"
version = "0.1.0"
description = "rerun 统一写入进程：订阅 Firefly/Viz 话题，写共享 viewer 或离线 rrd"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "firefly-mujoco",
    "iceoryx2==0.9.3",
    "numpy>=2,<3",
    "rerun-sdk>=0.36.2",
]

[tool.uv.sources]
firefly-mujoco = { workspace = true }

[build-system]
requires = ["uv_build>=0.9.18,<0.10.0"]
build-backend = "uv_build"

[project.scripts]
firefly-viz = "firefly_viz.main:main"
