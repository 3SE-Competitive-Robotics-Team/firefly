[project]
name = "firefly-mujoco"
version = "0.1.0"
description = "MuJoCo 物理环境库：无人机场景 + 传感器提取（IMU/双目灰度/深度渲染）"
readme = "README.md"
authors = [
    { name = "XiaoPengYouCode", email = "120481176+XiaoPengYouCode@users.noreply.github.com" }
]
requires-python = ">=3.12"
dependencies = [
    "mujoco>=3.11,<4",
    "numpy>=2,<3",
]

[build-system]
requires = ["uv_build>=0.9.18,<0.10.0"]
build-backend = "uv_build"
